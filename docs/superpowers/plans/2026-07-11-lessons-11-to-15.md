# Lessons 11–15 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add five tested interactive lessons for primes, prime factors, square numbers, cube numbers, and fractions.

**Architecture:** Extend the typed catalog and dispatch four focused scene components through the existing lesson player. Keep persistence data-driven so the current local-storage schema expands from ten to fifteen courses without migration.

**Tech Stack:** React 19, TypeScript, Vitest, Testing Library, CSS, vinext/Vite.

## Global Constraints

- Original 7–12-year-old learning content; do not reproduce source video artwork or script.
- Five interactive scenes plus one three-question quiz per course.
- Keyboard, pointer, and touch operation; no new dependencies or image assets.
- Existing progress and per-course/reset-all behavior remain compatible.

### Task 1: Catalog and types

**Files:** Modify `lib/course-types.ts`, `lib/courses.ts`, `lib/courses.test.ts`.

- [ ] Add a failing test expecting course IDs `primes`, `prime-factors`, `squares`, `cubes`, `fractions`, 15 total courses, and new kinds `prime`, `factor-tree`, `power`, `fraction`.
- [ ] Run `npm test -- lib/courses.test.ts` and confirm failure.
- [ ] Add exact course data from the approved design, five interactions and a three-question quiz each.
- [ ] Run the catalog test and commit with `feat: add lesson eleven through fifteen catalog`.

### Task 2: Interactive components

**Files:** Create `PrimeScene.tsx`, `FactorTreeScene.tsx`, `PowerScene.tsx`, `FractionScene.tsx`, and `LessonsElevenToFifteen.test.tsx` under `components/scenes/`.

- [ ] Write failing tests: 17 is prime after retrying 15; `30 = 2 × 3 × 5`; `3² = 9`; `3³ = 27`; `1/4 < 1/2`.
- [ ] Run the focused test and confirm missing-component failure.
- [ ] Implement scene-ID configuration, visual models, attempts, feedback, and one-shot completion.
- [ ] Run focused tests and commit with `feat: add prime power and fraction interactions`.

### Task 3: Integration and styling

**Files:** Modify `components/MathLab.tsx`, `components/MathLab.test.tsx`, `app/globals.css`.

- [ ] Add failing navigation assertions for lessons 11–15 and reset copy `15 节课程`.
- [ ] Run `npm test -- components/MathLab.test.tsx` and confirm failure.
- [ ] Import and dispatch the four new components; update chapter copy to cover fractions; keep counts derived from `courses.length`.
- [ ] Add responsive CSS for sieve, factor tree, power grids/cubes, and fraction bars.
- [ ] Run the full suite and commit with `feat: integrate lessons eleven through fifteen`.

### Task 4: Verify and publish

- [ ] Run `npm test`, `npm run lint`, `npm run build`, `npm run build:pages`, and `git diff --check`.
- [ ] Push `main`, publish `pages-dist` to `gh-pages`, and wait for Pages deployment success.
- [ ] Verify HTTP 200 and deployed JS contains `质数`, `质因数`, `平方数`, `立方数`, `分数`, and `15 节课程`.
